export enum Direction {
    Left = 'left',
    Right = 'right',
    Forward = 'forward',
    Backward = 'backward'
}

export enum GameStateEnum {
    startGame = 'start game',
    activeGame = 'active game'
}