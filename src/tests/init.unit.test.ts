// import { GameStateEnum } from "../gameState.js"
// import { init } from "../init.js"

// describe(`init()`, () => {
//     it(`should return a gameEntities object, a particle effects manager, a time tracker and the correct initial game state`, () => {
//         const initResults = init()

//         expect(initResults).toHaveProperty(`gameEntities`)
//         expect(initResults).toHaveProperty(`particleEffectManager`)
//         expect(initResults).toHaveProperty(`timeTracker`)
//         expect(initResults).toHaveProperty(`gameState`, GameStateEnum.activeGame) // should be start game when we have a start game screen up
//     })
// })