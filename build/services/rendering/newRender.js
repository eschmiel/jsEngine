// import { renderShip } from "../../entities/ship/renderShip"
// export function newRender(gameEntities, particleEffectManager, gameState) {
//     const {ships, bulletManagers, lives} = gameEntities
//     ships.forEach((ship, player) => {if(ship) renderShip(ship, player)})
//     bulletManagers.forEach((manager, player) => {
//         manager?.forEachBullet((bullet) => renderBullet(bullet, player) )
//     })
// }
//# sourceMappingURL=newRender.js.map